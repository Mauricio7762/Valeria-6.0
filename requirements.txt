# VALERIA 6.0 - Dependencias
# pip install -r requirements.txt

# Core (obligatorio para chat / razonamiento)
pydantic>=2.5
psutil>=5.9
loguru>=0.7
python-dotenv>=1.0

# Tests
pytest>=7.4
pytest-asyncio>=0.23

# UI (opcional)
streamlit>=1.30

# API (opcional)
fastapi>=0.109
uvicorn[standard]>=0.27
httpx>=0.27

# PDF
pypdf>=4.0

# Imagen (API y local)
pillow>=10.0

# LLM LoRA chat / extractores (opcional — GPU recomendada)
# torch: instalá la build adecuada desde https://pytorch.org si hace falta
torch>=2.1
transformers>=4.49.0
peft>=0.11
accelerate>=0.27
# bitsandbytes>=0.43  # solo cuantización 4/8-bit en GPU

# Visión local Qwen2.5-VL (español, sin API)
qwen-vl-utils>=0.0.8

# Audio local STT (opcional)
# faster-whisper>=1.0
