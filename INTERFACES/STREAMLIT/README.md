# UI Streamlit — VALERIA 6.0

```bash
pip install streamlit loguru rich pyyaml psutil
streamlit run INTERFACES/STREAMLIT/app.py
```

Abrí la URL (puerto 8501). El historial se guarda en `DATA/MEMORY/episodica/chat_ui.json`.
